services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: rinko
      POSTGRES_PASSWORD: rinko
      POSTGRES_DB: rinko
    ports:
      - "5432:5432"
    volumes:
      - rinko_db_data:/var/lib/postgresql/data

volumes:
  rinko_db_data:
